pub struct RenderCache {
    // cache
}

impl RenderCache {
    fn init() -> Self {
        // start thumbnail thread
        Self {}
    }
    fn draw(&mut self, state: AppState, pixels: &mut Pixels) {}
}
